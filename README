# The LewMC Website
Welcome to the GitHub repository for the lewmc.net website.

> This GitHub repository is likely to change often.

The website is written in PHP, JavaScript, and CSS (using Tailwind).

To change CSS you'll need Tailwind Installed. To do this run `npm install` then `npx tailwindcss -i './Assets/CSS/Dev.css' -o './Assets/CSS/Dist.css'`